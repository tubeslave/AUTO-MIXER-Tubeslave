# Offline controller delivery and fresh Ptitsa mix

User requests actual controller integration and a from-scratch Ptitsa mix/master, not another status-only cycle.

Bounded engineering scope: connect MasteringTargetController to a real file-oriented CLI, retaining exact input identity, candidate/rollback evidence, post-export metering, and pending human review. Do not replace the legacy live-compatible renderer or change live controls. Use existing dependencies and lossless source/checkpoint artifacts. Preserve originals and previously rejected mixes.

Validation: reachable synthetic render, rejected target returns exact source, input immutability, finite stereo-only input, no clobbering, WAV and encoded-MP3 remeasurement. Candidate search remains bounded. User audio remains an audition candidate, never an accepted baseline.

Preparation failure: first source checkpoint job 35923630925 failed at checkout because persist-credentials=false traversed a malformed AutomaticMixingPapers gitlink. Replacement 35923746548 uses an immutable public archive and saved all 18 lossless sources. No neural processing was repeated.
