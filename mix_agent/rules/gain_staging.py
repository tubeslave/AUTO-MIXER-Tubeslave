"""Gain-staging rule group placeholder module.

Rules are implemented in :mod:`mix_agent.rules.base` and grouped by issue id.
This module exists so integrations can import the canonical group name.
"""

RULE_GROUP = "A. gain_staging"
